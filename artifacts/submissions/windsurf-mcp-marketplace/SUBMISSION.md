# `windsurf` → `windsurf-mcp-marketplace` (ROI #5, P1)

No material gap. The one thing this package cannot give you is the intake URL.

Identity, copy, and assets: [MATERIALS.md](../MATERIALS.md). Do not retype them here.

## Where — unresolved, and deliberately left blank

This channel has **no `officialSource` in the SSOT**. Confirm the current intake route from
Windsurf's own documentation before submitting; do not trust a URL from memory or from a
third-party listing. When you find it, record it as `officialSource` on this channel in
`apps/web/data/distribution-surfaces.json` in the same edit that records the outcome — that
is how the next person avoids repeating this lookup.

Writing a plausible-looking URL here would be the same evidence-free claim the verdict
vocabulary forbids, in prose form. An unverified route is `UNKNOWN`, not a best guess.

## What to paste

- Name, description (short), category, tags — all from [MATERIALS.md](../MATERIALS.md)
- Repository: https://github.com/calllint/calllint
- MCP manifest: `packages/calllint-mcp/server.json`
- README to cite: `packages/calllint-mcp/README.md`
- Logo: `assets/brand/logo-mark-128.png`
- Windsurf-specific example config: `examples/mcp-configs/windsurf.json` — already in the
  tree, so the listing can point at a config a Windsurf user can paste directly

## Recording the outcome

Per [README.md](../README.md): edit only the SSOT, then regenerate. This channel is
`windsurf` → `windsurf-mcp-marketplace`. Add `officialSource` at the same time (see above).
