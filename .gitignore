# dependencies
node_modules/
.pnpm-store/

# build output
dist/
*.tsbuildinfo
apps/cli/logo-mark-128.png

# test / coverage
coverage/
.vitest/
corpus-summary.json

# caches
.calllint/
.cache/

# logs
*.log
npm-debug.log*
pnpm-debug.log*

# os / editor
.DS_Store
Thumbs.db
.idea/
*.swp

# local agent / editor config (not published)
.claude/
.cursor/
CLAUDE.md
.githooks/
scripts/install-githooks.mjs
scripts/strip-ai-commit-attribution.mjs

# internal planning notes — kept local, not published to the repo.
# (Public trust docs under docs/ — ADRs, threat model, corpus, release docs —
# are tracked intentionally; only these planning drafts stay local.)
000.md
*next.md
docs/v0.2.1 Hardening.md

# local Playwright/MCP verification artifacts (not published)
.playwright-mcp/
