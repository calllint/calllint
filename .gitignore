# dependencies
node_modules/
.pnpm-store/

# build output
dist/
*.tsbuildinfo
apps/cli/logo-mark-128.png

# test / coverage
coverage/
.vitest/
corpus-summary.json

# caches
.calllint/
.cache/

# logs
*.log
npm-debug.log*
pnpm-debug.log*

# os / editor
.DS_Store
Thumbs.db
.idea/
*.swp

# local agent / editor config (not published)
.claude/
.cursor/
CLAUDE.md
.githooks/
scripts/install-githooks.mjs
scripts/strip-ai-commit-attribution.mjs

# internal planning notes & docs — kept local, not published to the repo.
# The entire docs/ tree (ADRs, roadmap, threat model, plans, drafts) is now
# local-only; project-facts.json was relocated to the repo root as it is
# machine-read public metadata (corpus numbers, stable version, copy guard).
000.md
*next.md
docs/
PROJECT_STATUS.md

# local Playwright/MCP verification artifacts (not published)
.playwright-mcp/
