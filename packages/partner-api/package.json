{
  "name": "@calllint/partner-api",
  "version": "0.1.0",
  "private": true,
  "description": "Phase I2a read-only Partner API — a pure request router over the pre-baked, digest-addressed Trust Pages (ADR 0046 §4-§5, ADR 0038 §3-§4). Contains NO scanner: it only reads committed static artifacts, never resolves/fetches/scans.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@calllint/types": "workspace:*"
  },
  "devDependencies": {}
}
