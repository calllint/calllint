{
  "mcpServers": {
    "filesystem": {
      "command": "docker",
      "args": [
        "run",
        "-i",
        "--rm",
        "--mount=type=bind,src=/root,dst=/data",
        "mcp/filesystem",
        "/data"
      ]
    }
  }
}
