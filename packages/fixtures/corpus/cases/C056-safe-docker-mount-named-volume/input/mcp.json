{
  "mcpServers": {
    "cache": {
      "command": "docker",
      "args": [
        "run",
        "-i",
        "--rm",
        "--mount",
        "type=volume,src=appdata,dst=/data",
        "mcp/cache"
      ]
    }
  }
}
