{
  "schemaVersion": "calllint.corpus.case.v1",
  "caseId": "C023-safe-filesystem-docker-mount",
  "title": "Filesystem server via docker bind-mounts is SAFE (with a documented limit)",
  "curationStatus": "real-public-snapshot",
  "riskTheme": [
    "filesystem"
  ],
  "origin": {
    "kind": "public-repository",
    "description": "Public documentation config from modelcontextprotocol/servers (src/filesystem/README.md (Docker)). Content verbatim; only normalized to a valid JSON root. Upstream placeholders preserved.",
    "url": "https://github.com/modelcontextprotocol/servers",
    "commit": "7b1170d1da1e36bc9f553f51e76e64cbfd652b3e",
    "license": "Apache-2.0 / MIT (transition); docs CC-BY-4.0",
    "retrievedAt": "2026-06-17T00:00:00.000Z",
    "redactions": []
  },
  "input": {
    "path": "input/mcp.json",
    "format": "mcp-json"
  },
  "humanExpectation": {
    "verdict": "SAFE",
    "rationale": "The pinned image is given /projects inside the container; a reviewer reading the docker --mount sources would still want to check what host paths are bound, which the static scan does not currently inspect.",
    "knownLimitations": [
      "The broad-path detector reads the server's own path args (/projects), not docker --mount src= host paths — a documented false-negative surface."
    ]
  },
  "executionPolicy": {
    "executeTarget": false,
    "allowNetwork": false,
    "allowFilesystemMutation": false
  }
}
