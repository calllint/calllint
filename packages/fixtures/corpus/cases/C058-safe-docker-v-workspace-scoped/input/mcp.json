{
  "mcpServers": {
    "filesystem": {
      "command": "docker",
      "args": [
        "run",
        "-i",
        "--rm",
        "-v",
        "${workspaceFolder}/data:/data",
        "mcp/filesystem",
        "/data"
      ]
    }
  }
}
