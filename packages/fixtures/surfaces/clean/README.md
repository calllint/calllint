# Notes MCP

A simple, friendly notes server for storing and retrieving project notes.

## Usage

Configure it in your MCP client and call `save_note` to store a note or
`get_note` to retrieve one. Notes are kept in your workspace. Contributions are
welcome — see CONTRIBUTING.md. Supports UTF-8 content (café, naïve, Zürich).
